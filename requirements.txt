# BioX-A End-to-End POC -- Python dependencies
# Install with:  pip install -r requirements.txt
#
# Tested with Python 3.9+. Versions are lower-bounded (>=) to allow patch/minor
# updates while avoiding known-incompatible older releases. Pin exact versions
# (==) if you need a fully reproducible environment.

# --- Core data / DB ---
pandas>=2.0
numpy>=1.24
SQLAlchemy>=2.0
psycopg2-binary>=2.9

# --- Machine learning ---
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0
statsmodels>=0.14
shap>=0.44

# --- Causal uplift (optional: falls back to statsmodels OLS if absent) ---
econml>=0.15

# --- Visualization ---
matplotlib>=3.7
seaborn>=0.13
plotly>=5.18

# --- Experiment tracking & dashboard ---
mlflow>=2.9
streamlit>=1.30

# --- Notebook runtime ---
jupyter>=1.0
ipykernel>=6.0
